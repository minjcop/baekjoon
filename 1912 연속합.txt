//100000000 1000이 다 더해져도 이거라서 int사용 가능

dp[i] = a[i]로 모두 초기화

dp[n] // dp[n-1]이 0이상이라면 dp[n] += dp[n-1]
음수라면 dp[
그러고 마지막에 dp[i]를 1부터 n까지 조사해서 가장 큰 값을 출력한다.

주의) dp[i] 모든 값이 음수일 수 있으므로
초기화를 시킬때 dp[1]로 시켜줘야 한다.



		