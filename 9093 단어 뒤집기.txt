! abcde가 입력으로 들어올때 char c;  cin >> c; 를 이용해서 한단어씩 입력받을 수 있다.
! string s; 로 입력받아서 s[0] s[1] 등 배열처럼 사용가능하다.
! string s; cin >> s; 로 하면 단어단위로 입력을 받게 되는데, 

! getline(cin, s)를 이용하면 줄단위로 입력을 받을 수 있다. 
! getline은 마지막으로 입력받은 순간부터 \n까지 입력.
! 테스트케이스를 입력받았으므로 cin.ignore(); 로 기존의 버퍼에 있는 것을 없애줘야 한다.
! str+='\n';

문장이 주어졌을때 단어를 뒤집어서 출력하는 프로그램.
단어의 순서는 바꿀 수 없다.

테스트 케이스 T가 주어진다.
각 테스트 케이스는 한줄.
문장이 하나 주어진다.
단어의 길이는 최대 20,
문장의 길이는 최대 1000
단어와 단어 사이에 공백이 하나 존재한다.

단어만 뒤집는 것이다
Baekjoon online judge면
noojkeaB enilno egduj 로 바꾸면 된다.

공백을 입력받을때까지 계속 입력받고, [20]
공백을 입력받으면 뽑아내면 된다.

한개의 문자 단위로 입력을 받으면 되는데,
