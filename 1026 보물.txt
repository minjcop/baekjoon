순열을 사용하면 된다. (순열을 사용하게 되면 O(50!)이기 때문에 시간초과가 발생한다.

a에서 가장 큰 숫자와 b에서 가장 작은 숫자.
a에서 2번째로 큰 숫자와 b에서 2번째로 작은 숫자
이런식으로 구해줘서 다 곱하면 되는듯.
a에서 가장 작은 숫자와 b에서 가장 큰 숫자.

B를 재정렬할 수 없다고 했으므로 C라는 배열을 만들어서 B를 재정렬한것을 집어넣으면 된다.

내림차순 * 오름차순 = 최솟값이라는 결론에 도달했다.
