1부터 n까지의 수를 스택에 넣었다가 뽑아 늘어놓음으로써 하나의 수열을 만들 수 있다.
n(1 <= n <= 100000)
n이 주어지고
이후에 n개의 숫자가 주어진다.

push는 +로, pop은 -로 표현한다.
불가능한경우 NO를 출력한다.

int now = 0; // 현재까지 push된 가장 높은 수.
입력된 수와 같을때까지 push를 한다.
입력된 수가 now보다 작거나 같은 경우 top을 통해 pop을 한다.

루프를 다돌았는데 st.empty()를 해보았을때 스택이 비어있지 않으면 "NO"를 출력한다.

