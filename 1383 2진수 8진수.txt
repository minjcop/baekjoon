!char 를 int 형으로 바꾸면 아스키코드에 해당하는 수가 저장된다
'6' -'0' = 6이런 식으로 int 로 바꿔줘야 한다.


다른 사람 코드)
reverse(s.begin(), s.end()) 해서
while(s.size() % 3) s.push_back('0');

이런식으로 하고, 신기한건 뒤집고 난 후에 작업을 수행하는 것이 아니라 뒤집기 전에
s[i] * 1
s[i + 1]*2
s[i + 2]*4
이런 식으로 작업을 수행해서 8진수로 바뀐 문자열을 저장해준 후에 문자열을 뒤집었다는 것!
그래야 당연히 8진수니까 시간이 줄어들겠지?
good