() : VPS라고 부른다.
x가 VPS라면 (x)도 VPS
concatenation 시킨 xy도 VPS

YES, NO로 결과를 나타내야 한다.
열린괄호일때는 스택에 push를 하고
닫힌괄호일때는 top에 있는 값을 확인하고 그 값이 열린괄호면 (여기서는 '(' 하나뿐이기 때문에 그냥 
!st.empty()일때 pop을 해주면 될듯.

그렇게해서 입력을 다 받은 후에 
스택에 남은 문자열이 없으면 YES,
있으면 NO를 출력하면 된다.
